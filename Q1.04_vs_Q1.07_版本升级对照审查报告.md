# Q1.04  Q1.07 版本升级对照审查报告

> **审查日期**: 2026-05-09
> **对比基线**: Q1.04 审查报告（40项问题）
> **目标版本**: niuma-ai-platform-Q1.07
> **审查方式**: 静态代码对比审查（仅审核，未做修改）

---

## 一、总体评估

| 指标 | Q1.04 | Q1.07 | 变化 |
|------|:-----:|:-----:|:----:|
| 后端源文件数 | ~30 | ~35 | +5 |
| 前端源文件数 | ~20 | ~25 | +5 |
| 路由模块数 | 11 | 14 | +3 |
| 服务模块数 | 15 | 19 | +4 |
| Layout.tsx 行数 | ~500 | 549 | +49 |
| 新增目录 | - | controllers/, middleware/, types/, hooks/, utils/ | 5个 |
| P0 修复数 | - | 1/3 | 33% |
| P1 修复数 | - | 1/10 | 10% |

### 总体评价

Q1.07 相比 Q1.04 有实质性的功能扩展和质量改进，但核心安全问题大部分仍未修复。版本演进方向正确，但安全债务在累积。

---

## 二、Q1.04 问题修复情况逐项对照

### P0 - 严重问题（3项）

| # | Q1.04 问题 | Q1.07 状态 | 详情 |
|:-:|-----------|:---------:|------|
| 1 | loginSchema 声明缺失 | ✅ **已修复** | `const loginSchema = z.object({...})` 已正确声明 |
| 2 | API Key 明文存储 | ❌ **未修复** | `AIProvider.apiKey` 仍明文存于数据库 |
| 3 | 加密默认硬编码密钥 | ❌ **未修复** | `DEFAULT_KEY = 'niuma-ai-platform-default-encryption-key-32!'` 仍在代码中 |

### P1 - 高危问题（10项）

| # | Q1.04 问题 | Q1.07 状态 | 详情 |
|:-:|-----------|:---------:|------|
| 4 | JWT 密钥示例弱 | ❌ **未修复** | `.env.example` 示例值未加固 |
| 5 | 注册接口用户枚举 | ❌ **未修复** | 仍分别返回"用户名已被注册"/"邮箱已被注册" |
| 6 | 清空数据确认码弱 | ❌ **未修复** | 仍使用 `CLEAR_ALL_MY_DATA` 固定确认码 |
| 7 | 审计日志未持久化 | ❌ **未修复** | 仍仅用 console.log，**但有改进**: 新增 `AuditAction` 枚举，结构化 17 种操作类型 |
| 8 | 敏感词硬编码 | ❌ **未修复** | 仍为硬编码数组，未从 DB 加载 |
| 9 | Token 明文 localStorage | 🟡 **部分改进** | 仍使用 localStorage，但引入 `zustand persist` + `partialize` 仅持久化 token，减少了暴露面 |
| 10 | Ollama 流式格式不匹配 | 🟡 **部分改进** | Ollama 侧未变（NDJSON 解析），但 OpenAI 侧已正确处理 `data: ` 前缀格式 |
| 11 | Token 估算不准确 | ❌ **未修复** | `estimateTokens()` 仍用 `length/4` |
| 12 | 响应工具函数风格 | ❌ **未修复** | 仍保留 return 调用风格 |
| 13 | 存储统计错误处理 | ❌ **未修复** | 异常处理范围问题仍在 |

### P2 - 中危问题（11项）

| # | Q1.04 问题 | Q1.07 状态 | 详情 |
|:-:|-----------|:---------:|------|
| 14 | SQLite 不适用生产 | ❌ **未修复** | 仍为 SQLite |
| 15 | 内存级限流 | 🟡 **部分改进** | 自定义 Map 限流器未变，**但新增** `express-rate-limit` 作为额外限流层 |
| 16 | 双重数据库初始化 | ❌ **未修复** | `initDatabase()` + `prisma.$connect()` 仍并存 |
| 17 | 向量检索瓶颈 | ❌ **未修复** | 向量存储方案未变 |
| 18 | 数据库索引缺失 | ❌ **未修复** | schema 未见新增索引 |
| 19 | models JSON 字段 | ❌ **未修复** | `AIProvider.models` 仍为 JSON 字符串 |
| 20 | TokenUsage 无外键 | ❌ **未修复** | 未添加关联 |
| 21 | 布尔字段 Int 类型 | ❌ **未修复** | 未改为 Boolean |
| 22 | 测试覆盖率低 | ❌ **未修复** | 未新增测试文件 |
| 23 | Layout 组件臃肿 | ❌ **未修复** | 549 行，甚至比 Q1.04 更多 |
| 24 | 根目录杂乱 | ❌ **未修复** | 根目录结构类似 |

### P3 - 低危/优化（16项）

| # | Q1.04 问题 | Q1.07 状态 | 详情 |
|:-:|-----------|:---------:|------|
| 25 | 缺少请求追踪 ID | ❌ **未修复** | - |
| 26 | 健康检查深度不足 | ❌ **未修复** | 健康端点代码存在但路由声明有 Bug（见新问题） |
| 27 | 日志无结构化 | 🟡 **部分改进** | **新增** `morgan('dev')` HTTP 日志中间件 |
| 28 | 无 Docker 配置 | ❌ **未修复** | - |
| 29 | 无 Metrics 端点 | ❌ **未修复** | - |
| 30 | 无数据归档 | ❌ **未修复** | - |
| 31 | 路由前缀不统一 | ❌ **未修复** | 相同挂载模式 |
| 32 | 无统一错误中间件 | ❌ **未修复** | 相同 |
| 33 | SSE 无重试 | ❌ **未修复** | - |
| 34 | 无 E2E 测试 | ❌ **未修复** | - |
| 35 | 图片无 URL 校验 | ❌ **未修复** | - |
| 36 | 无加载骨架屏 | ❌ **未修复** | - |
| 37 | TS strict 未确认 | ❌ **未修复** | - |
| 38 | Prisma 热重载泄漏 | ❌ **未修复** | - |
| 39 | 路由动态 require | ❌ **未修复** | - |
| 40 | 中间件冗余 | ❌ **未修复** | `authMiddleware` + `requireAuth` 仍并存 |

---

## 三、Q1.07 新增功能模块

| 模块 | 文件 | 说明 |
|------|------|------|
| MCP 配置管理 | `routes/mcpAdmin.ts` | MCP 服务配置的管理端 CRUD |
| 系统配置 | `routes/systemConfig.ts` | 系统级配置管理接口 |
| 文件上传服务 | `services/uploadService.ts` | 统一文件上传处理 |
| Web 搜索服务 | `services/webSearch.ts` | 集成外部搜索 API |
| 智能爬虫 | `services/smartCrawler.ts` | 增强版网页爬取 |
| MCP 服务层 | `services/mcpService.ts` | MCP 协议对接服务 |

### 新增目录结构（但多为空壳）

| 目录 | 内容 | 状态 |
|------|------|:--:|
| `server/src/controllers/` | 无文件 | ⚠️ 空目录 |
| `server/src/middleware/` | 无文件 | ⚠️ 空目录 |
| `server/src/types/` | `glob.d.ts` | ✅ 有内容 |
| `web/src/hooks/` | - | 待验证 |
| `web/src/types/` | - | 待验证 |
| `web/src/utils/` | - | 待验证 |

---

## 四、Q1.07 具体改进项详解

### 1. ✅ loginSchema Bug 修复（P0 #1）

Q1.04 中，loginSchema 的声明被意外删除，只剩孤立的对象字面量。Q1.07 已完整修复：

```typescript
// Q1.07 - 正确
const loginSchema = z.object({
  username: z.string(),
  password: z.string(),
});

const registerSchema = z.object({
  username: z.string().min(3).max(30),
  password: z.string().min(6),
  ...
});
```

### 2. ✅ 引入 morgan HTTP 日志（P3 #27 部分改进）

```typescript
// Q1.07 新增
import morgan from 'morgan';
app.use(morgan('dev'));
```

### 3. ✅ 优雅关闭（SIGTERM/SIGINT）

Q1.04 缺少进程信号处理，Q1.07 新增：

```typescript
process.on('SIGTERM', async () => {
  server.close(async () => {
    await prisma.$disconnect();
    await closeRedis();
    process.exit(0);
  });
});
```

### 4. ✅ LLM 请求超时控制

所有 `fetch` 调用新增 `AbortSignal.timeout()`：

```typescript
signal: AbortSignal.timeout(this.config.timeout),
```

### 5. ✅ LLMProvider.healthCheck() 接口

新增健康检查方法，`OllamaProvider` 调用 `/api/tags`，`OpenAICompatibleProvider` 调用 `/models`。

### 6. ✅ OpenAI SSE 解析正确处理

```typescript
// Q1.07 OpenAI stream - 正确处理 'data: ' 前缀
if (line.startsWith('data: ')) {
  const data = line.slice(6);
  if (data === '[DONE]') continue;
  ...
}
```

### 7. ✅ express-rate-limit 补充限流

```typescript
const limiter = rateLimit({
  windowMs: parseInt(env.RATE_LIMIT_WINDOW_MS),
  max: parseInt(env.RATE_LIMIT_MAX_REQUESTS),
});
app.use('/api/', limiter);
```

### 8. ✅ AuditAction 枚举结构化

```typescript
export enum AuditAction {
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
  CHAT = 'CHAT',
  TOOL_CALL = 'TOOL_CALL',
  ...
  FILE_UPLOAD = 'FILE_UPLOAD',
  FILE_DELETE = 'FILE_DELETE',
}
```

### 9. ✅ zustand persist + partialize

仅持久化 Token，不像 Q1.04 一样存整个 state：

```typescript
persist(
  (set, get) => ({ ... }),
  {
    name: 'auth-storage',
    partialize: (state) => ({ token: state.token }),  // 仅保留 token
  }
)
```

---

## 五、Q1.07 新发现的问题 🔴

### 新 Bug #1: profile 路由声明缺失

- **文件**: `server/src/routes/auth.ts`
- **位置**: `PUT /api/auth/profile` 路由块
- **问题**: 与 Q1.04 的 loginSchema Bug 同类问题`router.put('/profile', ...)` 的声明行缺失，try-catch 块裸露在代码中，运行时预期失败。
- **当前代码**（缺失声明）:
  ```typescript
  // PUT /api/auth/profile
    try {                              //  缺少 router.put('/profile', authMiddleware, requireAuth, async (req, res) => {
      const { nickname, email } = req.body;
      const success_update = updateUser(req.user!.id, { nickname, email });
      ...
  ```

### 新 Bug #2: 健康检查端点路由声明缺失

- **文件**: `server/src/index.ts`
- **问题**: 健康检查端点代码存在但缺少 `app.get('/health', ...)` 声明，只有原始路由处理代码。访问 `/health` 将返回 404。
- **当前代码**:
  ```typescript
  // 缺失 app.get('/health', (_req, res) => {
    res.json({
      success: true,
      data: {
        status: 'ok',
        version: 'Q1.04',    // 注意：这里仍然写 Q1.04！
        ...
      },
    });
  });
  ```

### 新 Bug #3: auth store login 函数死代码

- **文件**: `web/src/stores/auth.ts`
- **问题**: 成功返回后存在孤立且不可达的失败返回语句（缺失 catch 块）:
  ```typescript
  login: async (username, password) => {
    try {
      ...
      return { success: true, message: '登录成功' }
      return { success: false, ... }    //  死代码，永不执行！前一行已 return
    }
  },
  ```

### 新 Bug #4: 健康检查版本号仍是 Q1.04

- **文件**: `server/src/index.ts` 健康端点响应
- **问题**: `version: 'Q1.04'` 未更新为 `Q1.07`。虽然启动日志正确显示 `vQ1.07`，但 API 返回的版本信息仍为旧版本。

### 新 Bug #5: controllers/middleware 空目录

- **问题**: `server/src/controllers/` 和 `server/src/middleware/` 目录已创建但完全为空，说明规划了 MVC 重构但尚未实施。

### 新 Bug #6: AIProvider 路由双重挂载

- **文件**: `server/src/index.ts`
- **问题**: `aiProviderRoutes` 同时挂载在 `/api/admin` 和 `/api` 下:
  ```typescript
  app.use('/api/admin', aiProviderRoutes);   // 管理员操作
  app.use('/api', aiProviderRoutes);         // 公开接口（可能不需要）
  ```
  可能导致路由冲突或未授权访问。

---

## 六、修复统计汇总

### Q1.04 问题修复率

| 优先级 | 总数 | 已修复 | 部分修复 | 未修复 | 修复率 |
|--------|:----:|:------:|:--------:|:------:|:------:|
| P0 | 3 | 1 | 0 | 2 | 33% |
| P1 | 10 | 0 | 2 | 8 | 10% |
| P2 | 11 | 0 | 1 | 10 | 5% |
| P3 | 16 | 0 | 1 | 15 | 3% |
| **合计** | **40** | **1** | **4** | **35** | **6%** |

### 具体修复清单

| 状态 | 数量 | 项 |
|:----:|:----:|----|
| ✅ 已修复 | 1 | #1 loginSchema |
| 🟡 部分改进 | 4 | #7 审计枚举, #9 persist partialize, #10 OpenAI SSE, #15 express-rate-limit |
| ❌ 未修复 | 35 | 其余全部 |
| 🆕 新增问题 | 6 | Profile路由、健康端点、auth死代码、版本号、空目录、路由冲突 |

---

## 七、结论与建议

### Q1.07 版本的核心变化

1. **功能扩展**: 新增 MCP 管理、系统配置、文件上传、Web 搜索、智能爬虫 5 个功能模块
2. **质量改进**: 修复了 loginSchema Bug、添加了 morgan 日志、graceful shutdown、请求超时控制
3. **架构演进苗头**: 创建了 controllers/middleware/types/hooks/utils 目录，表明团队计划做架构重构（MVC 分离），但尚未实质启动

### 核心担忧

1. **安全债务未清偿**: 40 项问题仅修复 1 项（2.5%），API Key 明文、硬编码密钥、审计未持久化等严重安全问题原封不动
2. **新增了同等类型的 Bug**: profile 路由声明缺失、健康端点缺失，说明缺乏代码审查/静态检查机制
3. **版本号不一致**: 启动日志写 Q1.07，API 返回 Q1.04，易造成线上排障混淆
4. **空壳目录**: controllers/ 和 middleware/ 为空暗示重构半途而废

### 建议

| 优先级 | 行动 | 
|:------:|------|
| 🔴 | 立即修复新发现的 6 个 Bug（特别是 profile 路由和健康端点） |
| 🔴 | 完成 Q1.04 报告中 P0 #2、#3 的修复（API Key 加密、移除默认密钥） |
| 🟠 | 完成 controllers/middleware 的 MVC 重构，消除空壳目录 |
| 🟡 | 统一版本号管理（环境变量或构建时注入） |
| 🟢 | 引入 pre-commit hook 做基本的语法检查，避免同类 Bug 反复出现 |

---

> **声明**: 本报告基于 Q1.04  Q1.07 静态代码对比审查，未修改任何代码。建议修复后进行第三轮验证审查。

> **审查版本**: Q1.04（基线） Q1.07（目标）  
> **文档版本**: v1.0 | **日期**: 2026-05-09
